export enum TransactionType {
  Deposit = "deposit",
  Withdraw = "withdraw",
  TransferIn = "transfer in",
  TransferOut = "transfer out"
}

export enum AccountType {
  Current = "current",
  Savings = "savings"
}